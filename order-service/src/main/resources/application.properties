# Application Name
spring.application.name=order-service

# Database Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/order_db
spring.datasource.username=carwash
spring.datasource.password=Root7879@123

# Server Port
server.port=8086

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.instance.prefer-ip-address=true

# Logging and SpringDoc OpenAPI Config
logging.level.org.springdoc=DEBUG
springdoc.show-actuator=true

# Spring Cloud Gateway (only needed if you're configuring gateway)
# These lines are not necessary in order-service unless it's a gateway itself.
# Commenting them out for clarity:
# spring.cloud.gateway.discovery.locator.enabled=true
# spring.cloud.gateway.discovery.locator.lower-case-service-id=true
